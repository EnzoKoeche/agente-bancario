[pytest]
# pythonpath=. põe a raiz do repo no sys.path para `pytest -q` resolver `import src`
# (o projeto roda os módulos via `python -m`, mas pytest puro não adiciona o CWD).
# Requer pytest >= 7.
pythonpath = .
testpaths = tests
