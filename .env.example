# Copie este arquivo para .env e preencha com a sua chave real.
# O arquivo .env NÃO deve ser commitado (já está no .gitignore).

# Chave de API da Anthropic (obrigatória para a extração via LLM).
# Obtenha em: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
