pydantic>=2.0
anthropic>=0.40
python-dotenv>=1.0

# Ingestão multi-formato (RF-01)
pypdf>=4.0                 # extração de texto de PDF (camada de texto)
# OCR de imagens (opcional) — requer também o binário Tesseract instalado no SO:
pytesseract>=0.3.10
Pillow>=10.0

# Testes (dev) — pythonpath no pytest.ini exige pytest >= 7
pytest>=7.0
